/** @type {import('next').NextConfig} */
const nextConfig = {
    // i18n: {
    //     locales: ['en', 'lv', 'ru'],
    //     defaultLocale: 'en',
    //     localeDetection: false,
    // }
}

module.exports = nextConfig
